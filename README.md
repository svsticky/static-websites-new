# Sticky's static websites

To run commands in either website's package.json, use the pnpm command:

```bash
pnpm intro-website build # to build the intro-website
pnpm main-website dev # to run the astro dev server for the main-website
# and so on...
```