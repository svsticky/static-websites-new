<nav>
    <img
        src="https://public.svsticky.nl/logos/logo_compact_outline_wit.svg"
        alt="Logo SV Sticky"
    />
    <ul>
        <li class="dropdown"><a href="index.astro">Home</a></li>
        <li class="dropdown"><a href="#vereniging">Vereniging</a></li>
        <li class="dropdown"><a href="#studie">Studie</a></li>
        <li class="dropdown"><a href="#carriere">Carrière</a></li>
        <li class="dropdown"><a href="#contact">Contact</a></li>
        <li class="dropdown"><a href="#links">Links</a></li>
        <li class="button"><a href="https://koala.svsticky.nl">Koala</a></li>
        <li class="button"><a href="https://wordlid.svsticky.nl">Word lid!</a></li>
    </ul>
    <a id="nav-icon">&#9776;</a>
</nav>

<script>
    document
        .getElementById("nav-icon")!
        .addEventListener("click", (_) =>
            document.querySelector("nav")!.classList.toggle("responsive"),
        );

    document.querySelectorAll("nav ul a").forEach((link) => {
        link.addEventListener("click", (_) =>
            document.querySelector("nav")!.classList.remove("responsive"),
        );
    });
</script>

<style lang="scss">

    html {
        scroll-behavior: smooth;
    }

    nav {
        background: #61518f;
        height: 4rem;
        display: flex;
        justify-content: space-between;
        padding: 1rem 1.5rem;
        width: calc(100vw - 3rem);

        img {
            height: 4rem;
            align-self: flex-start;
        }

        ul {
            display: none;
            list-style-type: none;
            margin: 0;

            li a {
                display: inline-block;
                color: white;
                padding: 3px;
                text-decoration: none;
                transition: transform 0.2s ease, opacity 0.2s ease;

                &:hover {
                    text-decoration: none;
                    opacity: 0.8;
                    transform: translateY(-3px);
                }
            }

            li.button {
                a {
                    background: #ffffff;
                    color: #61518f;
                    padding: 0.35rem 0.9rem;
                    border-radius: 10px;
                    font-weight: 600;
                    box-shadow: 0 2px 6px rgba(0,0,0,0.12);
                    transition: transform 0.15s ease, opacity 0.15s ease;
                    text-decoration: none;
                    display: inline-block;
                }

                a:hover {
                    opacity: 0.95;
                    transform: translateY(-2px);
                    text-decoration: none;
                }
            }
        }

        &.responsive ul {
            position: absolute;
            top: 6rem;
            left: 0;
            padding: 1rem;
            display: flex;
            z-index: 100;
            background: #61518f;
            width: calc(100vw - 2rem);
            flex-direction: column;
            gap: 1rem;
        }

        #nav-icon {
            align-self: flex-end;
            display: block;
            font-size: 1.8rem;
            color: white;
            cursor: pointer;
            margin: auto 0;
            text-decoration: none;
            user-select: none;
        }
    }

    @media screen and (min-width: 805px) {
        nav {
            padding: 1rem 0;
            width: 100vw;
            align-items: center;
            justify-content: space-around;

            #nav-icon {
                display: none;
            }

            ul {
                display: flex;
                gap: 2rem;
                margin: auto 0;
                padding: 0;
                align-self: flex-end;
            }
        }
    }
</style>
